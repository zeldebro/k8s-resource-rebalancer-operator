apiVersion: v1
kind: Service
metadata:
  labels:
    app: rebalancer
    app.kubernetes.io/name: rebalancer
    app.kubernetes.io/managed-by: kustomize
  name: rebalancer-svc
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app: rebalancer
